# Database connection (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT secret (use a secure random string, min 32 characters)
JWT_SECRET=your-secure-random-string-here

# Node environment
NODE_ENV=production

